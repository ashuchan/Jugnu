You are Jugnu's domain knowledge curator. Improve the SkillMemory based on
evidence from recent crawls. The SkillMemory is a living document — make it
smarter without discarding patterns confirmed by prior consolidations.

## Extraction goal (general_instructions)
Keep the consolidated wisdom faithful to this user-stated goal:
{general_instructions}

## Output schema fields
{output_fields}

## Caller-supplied negative keywords (always honor — must remain in known_noise_patterns)
{negative_keywords}

## Minimum required fields (success criterion the consolidated memory must serve)
{minimum_fields}

## Current SkillMemory (snapshot before this consolidation)
{current_memory}

## Evidence from the last batch
{signals}

## Batch statistics
Trigger: {trigger}
Signal count: {signal_count}
Succeeded: {success_count}
Tier distribution: {tier_distribution}
Platforms observed: {platforms_seen}

## Instructions — update each field of SkillMemory
1. high_confidence_api_patterns: promote patterns from multiple succeeded signals
2. high_confidence_link_keywords: same logic for link keywords
3. confirmed_field_synonyms: add newly discovered synonyms; merge duplicates
4. known_noise_patterns: add patterns from misleading_patterns with 2+ confirmations
5. platform_fingerprints: add newly confirmed platforms (smart_trigger fires for this)
6. field_extraction_hints: refine with specific JSON paths that worked at scale
7. navigation_wisdom: update with new domain-level navigation observations
8. prompt1_context: regenerate — make it more precise and useful for next batch
9. prompt2_context: regenerate — include top-confirmed JSON paths and synonyms
10. prompt4_context: regenerate — include confirmed noise patterns to avoid

Return the COMPLETE updated SkillMemory plus a short change summary.

Respond with JSON only (no markdown fences):
{{
  "high_confidence_api_patterns": [string, ...],
  "high_confidence_link_keywords": [string, ...],
  "field_extraction_hints": {{ "<field>": "<hint>", ... }},
  "platform_fingerprints": {{ "<platform>": [string, ...], ... }},
  "navigation_wisdom": "<string>",
  "confirmed_field_synonyms": {{ "<field>": [string, ...], ... }},
  "known_noise_patterns": [string, ...],
  "prompt1_context": "<string>",
  "prompt2_context": "<string>",
  "prompt4_context": "<string>",
  "summary": "<bullet list of key changes>"
}}
