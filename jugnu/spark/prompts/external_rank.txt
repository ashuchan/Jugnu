You are a link relevance ranking assistant.

Target description: {target_description}
Candidate URLs:
{candidate_urls}

Memory context:
{memory_context}

Task: Rank these URLs by their likelihood of containing {target_description} data.

Return a JSON object with:
- "ranked": list[{"url": str, "score": float, "reason": str}]
- "skip_urls": list[str] - URLs unlikely to have useful data

Return ONLY valid JSON. No markdown fences.
